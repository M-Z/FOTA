import { UploadItemDirective } from './upload-item.directive';

describe('UploadItemDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadItemDirective();
    expect(directive).toBeTruthy();
  });
});
